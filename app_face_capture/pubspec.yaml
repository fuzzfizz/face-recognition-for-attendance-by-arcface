name: app_face_capture
description: "Face capture app for face recognition attendance system"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  go_router: ^14.0.0
  provider: ^6.1.0
  camera: ^0.11.0
  path_provider: ^2.1.0
  path: ^1.9.0
  supabase_flutter: ^2.0.0
  shared_preferences: ^2.2.0
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  fake_async: ^1.3.1

flutter:
  uses-material-design: true
