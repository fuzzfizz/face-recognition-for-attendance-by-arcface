version: '3.8'

services:
  web_dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: faceattend_web_dashboard
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - DB_HOST=136.110.6.161
      - DB_PORT=3306
      - DB_USER=face_admin
      - DB_PASS=SecurePassword123!
      - DB_NAME=face_attendance
      - AI_SERVER_URL=http://34.124.240.7:8000
      - ADMIN_API_KEY=my-secure-admin-token-12345
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"